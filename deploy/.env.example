# Copy to  .env  (gitignored — docker-compose reads it automatically). Never commit real values.
HESTIA_INFLUX_TOKEN=your-influxdb-token-scoped-to-the-hestia-bucket
HESTIA_INFLUX_ORG=helios
HESTIA_INFLUX_BUCKET=hestia
