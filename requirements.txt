# This is what you can install on YOUR machine to develop + run the tooling locally:
#   python -m venv venv && venv\Scripts\Activate.ps1     (Windows PowerShell)
#   pip install -r requirements.txt
#
# Firmware flashing uses ESPHome, installed as an isolated CLI tool, not here:
#   pipx install esphome        (mixing its heavy pinned deps into this env causes conflicts)

# ── EPICS control layer (Channel Access) ──────────────────────────────────────
caproto>=1.1        # soft IOC + CA clients: the sim IOC (puck_ioc.py --sim) and the GUI

# ── USB serial bridge (plugged-in puck → EPICS, offline) ──────────────────────
pyserial            # for the planned puck_ioc.py --serial COMx mode

# ── Analysis + calibration notebooks (analysis/, calibration/) ────────────────
numpy
scipy
matplotlib
pandas
jupyter

# ── Concept animations for the write-up / video (visualization/) ──────────────
manim

# ── GUI ───────────────────────────────────────────────────────────────────────
# tkinter ships with Python — no install needed (used by gui/hestia_gui.py)

# ── Project-specific, uncomment as needed ─────────────────────────────────────
# influxdb-client   # only if running the ca2influx bridge outside Docker
# lmfit             # curve fitting with bounds (calibration)
# uncertainties     # error propagation (calibration / analysis)
